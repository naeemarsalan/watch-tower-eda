- name: Bring up AAP Site 2
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Scale AAP CR replicas according to role. 
      redhat.openshift.k8s:
        state: present
        src: ../files/aap-cr-up-site2.yml
        api_key: "{{ openshift_auth_results.openshift_auth.api_key }}"
        validate_certs: false